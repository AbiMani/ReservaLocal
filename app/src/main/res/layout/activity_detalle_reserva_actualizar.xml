<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:stretchColumns="1" >

    <TableRow>

        <TextView android:text="Id reserva de evento : " />

        <Spinner
            android:id="@+id/spinnerREv"
            android:inputType="number"
            />
    </TableRow>

    <TableRow>

        <TextView android:text="Id Horario : " />

        <EditText
            android:id="@+id/editidHorario"
            android:inputType="number"
            />
    </TableRow>

    <TableRow>

        <TextView android:text="Codigo Local : " />

        <EditText
            android:id="@+id/editcodigoLocal"
            android:inputType="text"
            android:maxLength="10" />
    </TableRow>

    <TableRow>

        <Button
            android:onClick="actualizarDetalleReserva"
            android:text="@string/actualizar" />
    </TableRow>

    <TableRow>

        <Button
            android:onClick="limpiarTexto"
            android:text="@string/limpiar" />
    </TableRow>

</TableLayout>